# SPEC.md — aozora-sakuin（青空索引）

<!-- scaffold template v1.12.0 から展開(2026-08-26)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

青空文庫の 5,000 作品を単一の全文索引に統合し、任意の語句が**どの作品でどう使われているか**を
前後文脈つきの用例集（KWIC）として引けるようにする。フリートの各アトラスが「一作・一作家の中を
掘る」道具であるのに対し、本プロジェクトは「作家と時代をまたいで語を横に読む」道具である。

索引の構築（接尾辞配列 SA-IS）とブラウザ側の照合（FM-index）はいずれも Rust で実装する。
これは速度のためではない。素の接尾辞配列は 4 バイト整数 × 1.6 億バイト = 約 640 MB となり
配信不可能で、BWT と疎な SA サンプルによる圧縮索引でなければ成立しないためである。
その構築とビット単位の rank/select は JavaScript では現実的に書けない。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 収録 5,000 作を決定論的に選定する（母集団 17,343 作・**アクセスランキング順** × 1 著者 16 作上限・不足分を 17 作目で補充） | must |
| F-02 | 選定作の本文を取得し、凡例・奥付・ルビ・入力注記を除去して正規化する | must |
| F-03 | 正規化本文から FM-index（BWT + ハフマン形ウェーブレット木 + 疎 SA 標本）を構築する | must |
| F-04 | ブラウザ側の照合器を wasm32 で提供し、語句の全出現位置を求める | must |
| F-05 | 一致語を中央に揃えた KWIC 表示（前後文脈・出典・位置）を出す。前後文脈は**索引だけから復元**する（原文は配信しない） | must |
| F-06 | 著者・分類（NDC）・文字遣いで絞り込み、件数分布を出す | must |
| F-07 | 旧仮名と新仮名の表記ゆれを吸収する（収録の 39% が旧仮名のため必須）。規則の妥当性は台帳の**文字遣い種別**で較正する | must |
| F-08 | 索引をシャードに分割し、HTTP Range で必要範囲だけ取得する | must |
| F-09 | 該当作を収録しているフリート内アトラスへ導線を張る | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 索引の構築・照合ロジックは全て Rust。JS は表示とグルーのみ | 照合・索引構造を扱う JS がないことを検査 |
| N-02 | JS は索引の内部構造を知らない。位置計算は全て wasm 側 | 境界を `extern "C"` の整数関数に限定 |
| N-03 | 本文取得は 0.8 秒以上の間隔・連絡先入り UA・レジューム可 | pipeline のテスト |
| N-04 | Vercel 静的配信のみ。サーバ側処理と運用コストを持たない | vercel.json に framework なし / 本番で `scripts/verify_prod.mjs` |
| N-05 | 配信ファイルは 1 本あたり 100 MB 未満（GitHub の制限） | ビルド後の実測 |
| N-06 | 収録の部分性（選定規則・非収録作の存在）を画面に明示する | 目視 + テスト |

## 4. 品質基準

**オラクル（各ループで必ず回す）**

| ID | 検査 | 基準 |
|---|---|---|
| O-01 | FM-index の検索結果と、`str::find` による総当たり検索の照合 | 全クエリ・全ヒット位置が**完全一致** |
| O-02 | BWT の逆変換 | 復元結果が正規化本文と**バイト単位で一致** |
| O-03 | Range 取得時の結果と、索引全体をメモリに置いた場合の結果 | **完全一致** |
| O-04 | 選定の決定論 | 再実行で `data/selection.tsv` の sha256 が不変 |
| O-05 | 配信量 | 1 シャード < 8 MB / 初回転送(常駐領域の総計) < 20 MB |
| O-09 | 配信形式に書き出して読み直した索引と、メモリ上の索引の照合 | 全クエリ・全ヒット位置が**完全一致** |
| O-10 | 全シャードの件数の合計と、全文に対する総当たりの照合 | **完全一致** |
| O-11 | 索引だけから復元した本文と、原文の照合 | 全区間が**バイト単位で一致** |
| O-12 | 配信形式の指紋（固定入力を直列化したバイト列のハッシュ） | 版と対応（並びを変えたら落ちる） |
| O-13 | wasm 境界越しの件数・用例と、総当たり探索の照合 | **完全一致** |
| O-14 | 実際の wasm を Node で動かした結果と、正規化本文の照合 | 用例が本文に**そのまま現れる** |
| O-15 | 異体形の展開（決定論・上限・語中限定・既知の対応） | 実行ごとに不変 / MAX_FORMS 以内 |
| O-17 | 規則表の old_ratio と、全 5,000 作での実測値の照合 | **±1 点以内で一致** |
| O-06 | ビット列の rank1 と、総当たり計数の照合 | 全位置で**完全一致** |
| O-07 | ウェーブレット木の rank/access と総当たりの照合 | 全位置・全文字で**完全一致** |

**選定規則（v2）と、v1 を捨てた理由**

v1 は「作品 ID 昇順に 1 著者 16 作まで」だった。決定論的で恣意も入らないが、
**作品 ID は登録順であって重要度ではない**（多作家では登録バッチ内の読み仮名順に近い）。
実測すると代表作 21 点のうち採録は 11 点で、芥川竜之介は羅生門も蜘蛛の糸も地獄変も入らず
「十本の針」「あばばばば」が入っていた。**決定論は選定の質を保証しない。**

v2 は順序に青空文庫自身の**アクセスランキング**（2009-01 以降の月次 164 本・各 500 件・
異なり 5,966 作・`data/ranking.json`）を使う。公開された外部指標なので恣意は入らず、
決定論も保たれる。差し替えで代表作の採録は **21/21**、著者 921 名の網羅も維持。

1. 母集団 = 著作権フラグ「なし」× テキスト zip あり = 17,343 作
2. 第 1 周: 順位スコア降順（同点は作品 ID 昇順）に 1 著者 16 作まで
3. 第 2 周: ランキングに無い作品を作品 ID 昇順に 1 著者 16 作まで
4. 第 3 周: 5,000 に足りない分を 17 作目から補充（同じ順序規則）

上限 16 作は代表性より著者網羅を優先する判断なので、上位 200 位のうち 11 作
（芥川「杜子春」など）は上限に阻まれて落ちる。上位 100 位は全て採録される。

**選定ゲート（G-01〜G-14・`tests/test_selection.py`）** — 行数 5,000 / 作品 ID 重複なし /
1 著者 17 作以下 / 17 作の著者ちょうど 75 名 / 著者 921 名全員 / 全行 .zip URL /
著作権フラグ全て「なし」 / 作品名空欄なし / 再実行で同一 / 作品 ID 昇順 / URL は aozora.gr.jp のみ /
**ランキング上位 100 位は必ず採録** / **代表作 21 点を名指しで固定** / 順位スコア列が台帳と整合

**実測に基づく規模の見積もり（`data/probe.json`・無作為 100 作・seed 20260826・失敗 0）**

- 正味本文 1 作あたり 平均 11,003 字 / 中央値 4,297 字 / UTF-8 2.93 バイト per 字
- 生テキストからの残存率 86.1%（全 999 作の実測でも 86.5%）
- 5,000 作の総量 161 MB（95% 区間 109〜224 MB）

**索引サイズの実測（20 MB の実本文で測定・見積もりではない）**

当初「FM-index は原文の 50〜70%」と書いたのは根拠のない見積もりで、平衡ウェーブレット木の
実測は **1.320 倍**だった（5,000 作なら 213 MB で配信不能）。木の形を頻度に合わせ、
SA 標本を行側で取ることで **0.694 倍**まで下げた。

| 構成 | 原文比 | 5,000 作 | 状態 |
|---|--:|--:|---|
| 平衡 WT + 標本ビット列 + SA 標本 32 | 1.320 | 213 MB | 破棄 |
| **ハフマン形 WT + 行標本 + SA 標本 64** | **0.694** | **112 MB** | 採用（実測） |

- 本文のバイト単位 0 次エントロピー **4.72 ビット/バイト**（異なるバイト値 158 種）に対し、
  実際の平均符号長は **4.75 ビット/バイト**（ハフマンの理論限界にほぼ到達）
- 索引構築 20 MB で 6.7 秒（線形）
- **F-08 のシャード分割は must** — 112 MB は単一ファイルで配信できない（GitHub は 100 MB 超を拒否）

**位置標本ではなく行標本にした代償（O-08）**

位置側で標本を取れば「SA 標本間隔 以内に必ず当たる」保証があるが、どの行が標本かを示す
ビット列（原文の 13.3%）が要る。行側で取ればその 13.3% が消える代わりに、**歩数の理論上界が無くなる**。
実測で押さえる方針とし、20 MB の実本文で **最大 846 歩**（SA 標本 64 の 13 倍）を確認した。
回帰検出のため `SA_SAMPLE × 100` を上界とするゲートを置いている。

**探索の実測（20 MB・ハフマン形・SA 標本 64）**

| 語 | 件数 | 件数取得 | 全位置復元 |
|---|--:|--:|--:|
| あはれ | 69 | 0.003 ms | 1.7 ms |
| うつくしい | 32 | 0.016 ms | 1.2 ms |
| 東京 | 664 | 0.016 ms | 16.0 ms |
| の | 284,527 | 0.006 ms | 6,850 ms |

- **件数はヒット数に依存せず一定**（0.003〜0.016 ms）。符号が短くなり平衡形より速くなった
- 位置の復元はヒット数に比例する。したがって**画面に出す分だけ復元する**（KWIC 1 画面 = 20 件程度）

**配信の実測（選定 v2・全 5,000 作・シャード 53 枚・ISA 標本つき）**

| | 実測 | 基準 |
|---|--:|--:|
| 収録 | 5,000 作 / 著者 921 名 | — |
| 正味本文 | 302.9 MB（102,352,565 字・残存率 87.1%） | — |
| シャード数 | 53 枚 | — |
| 1 シャード | 最大 4.7 MB | < 8 MB ✓ |
| 索引 総計 | 232.0 MB（本文の 0.766 倍） | サーバ側のみ |
| **初回に落とす資材**（wasm + 作品台帳 + manifest） | **0.28 MB** | < 1.5 MB ✓ |
| **1 クエリの追加転送** | **0.08〜0.14 MB** | — |
| シャード構築 | 38 秒 | — |
| 文脈の取り出し | 0.4〜2.5 ms（3〜4 件） | — |

索引の総計 232.0 MB はサーバに置くだけで、ブラウザへは配らない。経路を 2 つに分ける。

**件数 — HTTP Range で必要なバイトだけ読む（F-08 / O-03）**

各シャードの先頭にある常駐領域（作品表・木の形・rank 標本・C 表 = 索引全体の 5.1%）を
初回に読んでおき、後方探索の途中で足りないワードだけを Range で取りに行く。

| | 実測（全 5,000 作・53 枚・実 HTTP 越し） |
|---|--:|
| 常駐領域（初回のみ） | **11.9 MB** |
| 1 クエリの追加転送 | **0.08〜0.14 MB** |
| 1 シャードあたりの往復 | 27〜54 回 |
| 要求数 | 2,000〜3,600 |
| 索引全体との比 | **19 分の 1** |

常駐領域だけで答えが出る近道を 2 つ置いている。**このシャードに現れないバイトが
パターンに 1 つでもあれば 0 件**（C 表だけで分かる）。**後方探索の最初の 1 歩**も
`rank(c,0)=0` と `rank(c,行数)=その文字の総数` で、どちらも C 表から出る。

**用例 — 該当シャードを丸ごと落とす**

位置復元と文脈取り出しは LF を数百段も逐次に辿るので、Range では往復が過大になる。
件のあるシャードだけ丸ごと（最大 4.7 MB）取りに行き、逐次の読みをメモリ内に閉じる。

**シャード数の逆向きの関係（loop_004 の発見）**

後方探索の逐次段数（語のバイト数 × 木の深さ ≒ 45）は 1 シャードあたり一定なので、
**要求数 = シャード数 × 45**。したがって：

| シャード | 1 枚の大きさ | Range の要求数 | 用例 1 枚の転送 |
|---|--:|--:|--:|
| 228 枚 | 1.6 MB | 約 10,000 | 1.6 MB |
| **53 枚** | **4.4 MB** | **約 2,300** | **4.7 MB** |

**小さいシャードは丸ごと配信に有利だが、Range 計数には不利**。loop_003 で
「最初の用例が早く出る」ことを理由に 53 → 228 枚へ割ったが、Range 経路では裏目になる。
計数が Range で済むようになった以上、53 枚に戻すのが正しい。転送量だけを見て
要求数を見落としていた。

**原文を配信しない（F-05 / O-11）**

前後文脈は索引だけから復元する。LF は本文を後ろから前へ読むので、位置 → 行の標本
（ISA 標本）で「取り出したい範囲より後ろにある既知の行」へ跳び、そこから前へ辿る。
辿る歩数は `文脈長 + ISA_SAMPLE` を超えない。索引は 0.703 → **0.766 倍**（213 → 232 MB）に
増えるが、その代わり**索引が本文を兼ねる**。

**横断照合（O-10）** — 4 語すべてで 53 枚の件数合計が全文の総当たりと完全一致
（あはれ 1,197 / うつくしい 439 / 東京 12,269 / 吾輩 1,146）。
「吾輩」の最頻出は作品 ID 789『吾輩は猫である』481 件、次いで 2093『ドグラ・マグラ』341 件。
選定 v1 では 1 位が陸羯南『近時政論考』70 件で、『吾輩は猫である』は未収録だった。

**ブラウザとの境界（F-04 / N-01 / N-02）**

`extern "C"` の整数関数のみ。wasm-bindgen は使わない。JS が触れるのは
「シャードのバイト列を渡す」「語を渡す」「結果のレコード列を読む」の 3 つだけで、
BWT もウェーブレット木も LF 写像も境界の向こう側にある。位置の計算も文脈の取り出しも
すべて Rust 側で行う。

| 関数 | 役割 |
|---|---|
| `az_alloc` / `az_free` | 領域の確保と解放 |
| `az_shard_load` / `az_shard_drop` | シャードを読み込み、取っ手を返す |
| `az_shard_docs` | そのシャードの作品数 |
| `az_count` | 語の出現回数（位置は求めない） |
| `az_kwic` | 前後文脈つきの用例をレコード列として詰める |
| `az_out_ptr` / `az_out_len` | 詰めた結果の位置と長さ |
| `az_format_version` | 配信形式の版（取得したファイルと照合できる） |

レコードの並び（JS が知っていればよいことの全て）:
`u32 作品ID / u32 作品内位置 / u32 前文脈長 / u32 一致長 / u32 後文脈長 / 本文(UTF-8)`

**実測** — `web/wasm/sakuin.wasm` は **42,956 バイト**。Node 上でシャード 6 枚
（455 作）を読み、用例 2,553 件すべてが正規化本文にそのまま現れることを確認（445 ms）。

**表記ゆれの吸収（F-07 / O-15 / O-17）**

収録の 39% が旧仮名（新字旧仮名 25% + 旧字旧仮名 15%）なので、「あわれ」で引いて
「あはれ」が出ないのでは横断索引として成立しない。

歴史的仮名遣いへの変換は本来、語の切れ目と語種の知識を要する。それを辞書で与えると
辞書が「正しさ」の根拠になって検証が循環する（スコープ外）。そこで**機械的な置換だけ**を
行い、規則の妥当性は台帳の**文字遣い種別**（青空文庫が各作品に付けている独立した外部
ラベル）で較正する。異体形の出現が旧仮名の作品に偏っていれば本物の仮名遣い、偏って
いなければ助詞の境界などとの偶然の衝突である。

**較正の実測（全 5,000 作・`scripts/calibrate_variants.py` → `data/variant_calibration.json`）**

| 規則 | 旧仮名の作品に落ちる割合 | 件数 | 既定 |
|---|--:|--:|:--|
| い→ゐ | 99% | 58,583 | 有効 |
| う→ふ（語中） | 96% | 47,777 | 有効 |
| 長音 よう→やう | 99% | 46,152 | 有効 |
| 長音 そう→さう | 99% | 8,394 | 有効 |
| わ→は（語中） | 84% | 1,606 | 有効 |
| ず→づ | 80% | 1,295 | 有効 |
| お→ほ（語中） | 77% | 3,361 | 有効 |
| **お→を** | **37%** | 937 | **無効** |
| **え→へ（語中）** | **15%** | 12,386 | **無効** |
| **か→くわ** | **5%** | 705 | **無効** |

閾値は **70%**。実測は 77% と 37% の間に大きな空きがあり、どこで切っても同じ分け方になる
（ゲート V-04 がこの空きを 20 点以上と要求し、詰まってきたら落ちる）。

下 3 つは**規則としては正しい**（をんな・かをりは本物の異体形）が、語の切れ目を知らない
部分文字列照合では「をもう」が本文の「…を、もう…」に当たってしまう。解けない以上、
**既定では外し、形ごとに件数を見せて利用者が選べるようにする**。隠さずに出す。

実測の効果: あわれ 1,151 → 2,348 件（+104%）／ ように 93,707 → 120,316 件（+28%）／
しずか 971 → 1,621 件（+67%）。

**画面（F-05 / F-06）**

`web/` に素の HTML / CSS / モジュール JS。framework なし・Vercel 静的配信のみ（N-04）。

- 中央は検索結果の一覧ではなく**用例集（KWIC）** — 一致語を中央に揃え、語の据わり方を縦に読み比べる
- 左に**当たった形**（入力語 + 異体形、それぞれの件数と較正値）と、文字遣い・分類での絞り込み
- 右に作品別の件数と、索引そのものの諸元
- 走査は 1 枚ずつ進み、途中で止められる。件数が走査済みの範囲での値であることを常時表示

**JS は索引の構造を知らない（N-01 / N-02）** — ゲート W-03 が、`app.js` のコード（コメントを除く）に
BWT・ウェーブレット木・接尾辞・rank などの内部語が現れないこと、`az_` で始まる輸出以外を
触っていないことを検査する。

**本番の実測（https://aozora-sakuin.vercel.app）**

| | 実測 |
|---|--:|
| 索引の目次（初回のみ） | 11.9 MB / 53 要求 / 3.5 秒 |
| 1 クエリの追加転送 | **0.08〜0.09 MB** / 約 2,000 要求 / **2.3〜2.5 秒** |
| 件数 | 手元の実測と**完全一致**（あはれ 1,197 / 吾輩 1,146） |
| 総転送 | 12.0 MB（索引全体 232.0 MB の **19 分の 1**） |

Vercel の静的配信は Range に応じる（206 + Content-Range）。設計の前提が本番で成立している。

**品質ゲート** — `python scripts/verify.py` が Python ゲート / cargo fmt / clippy -D warnings /
cargo test / wasm ビルド / Node での wasm 実地検査 / **ローカル配信を立てての画面の実地検査**を
一括で回す。

## 5. スコープ外

- **形態素解析・品詞付与** — 辞書に依存した瞬間、辞書が「正しさ」の根拠になり検証が循環する（onomato-atlas の教訓）
- **著作権フラグ「あり」の 928 作** — 母集団から除外済み
- **全文リーダー** — 作品を通読させる機能は各アトラスに委ねる。本プロジェクトは用例までを出す
- **母集団 17,343 作の全収録** — 索引が 500 MB 規模になり配信不能
- **意味的検索・類似度** — 引けるのは実際に本文に現れる文字列のみ
